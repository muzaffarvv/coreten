spring:
  application:
    name: coreten-service

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 15MB # Increased slightly for request overhead

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/coreten}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:admin}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10 # Recommended for standard small services

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false # Performance: Prevents holding connections too long
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    naming:
      physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy

# Move custom logic under a unique namespace
app:
  file:
    upload-dir: ${UPLOAD_DIR:uploads}
    max-size-bytes: 10485760
jwt:
  secret: ${JWT_SECRET:ASCHBACSYEFQKLFASHKBCASCBJXDE@#0908ASKJCNAKSJCBKASJBCKASCJVASHCAKSBCKJASC8878ASCKJ}
  issuer: ${JWT_ISSUER:coreten-service}
  token-type: Bearer

  # 3 600 000 ms = 60 minut = 1 soat
  access-token-expiration-ms: ${JWT_ACCESS_EXPIRATION:3600000}

#    # 30 000 ms = 30 soniya
#    access-token-expiration-ms: ${JWT_ACCESS_EXPIRATION:30000}

  # 604 800 000 ms = 10 080 minut = 168 soat = 7 kun
  refresh-token-expiration-ms: ${JWT_REFRESH_EXPIRATION:604800000}

logging:
  level:
    root: INFO
    uz.vv.coretenservice: DEBUG
    org.springframework.security: INFO